# Build artifacts
target/
**/target/
# Transient test/CI logs
*.log
orch/e/
# Local env
*.env
!/vmm/guest/kernel-version.env
.DS_Store

# Quickstart guest assets (built kernel + pulled rootfs; large, regenerated by make guest)
/guest-assets/

# Local working notes (kept on disk, not published)
/notes/

# Library lock generated by local SDK test environments; published dependencies
# remain constrained in sdk/python/pyproject.toml.
/sdk/python/uv.lock
